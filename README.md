This repository contains the code for strand displacement POC.

The idea is to use DNA nanostructures as a support for DNA strands, that in turn contain information.

The encoding of data therefore has to support sufficient dissimilarity, so that when we want to invade one site, we don't accidentally invade another site.
